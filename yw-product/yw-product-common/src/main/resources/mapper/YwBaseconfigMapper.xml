<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunwei.product.common.dao.YwBaseconfigDao">

    <!-- 过滤查询结果(主要用于联合查询) -->
    <resultMap type="com.yunwei.product.common.model.YwBaseconfig" id="resultMap">
         <id column="id" property="id" />
         <result column="config_id" property="config_id" />  
         <result column="config_name" property="config_name" />  
         <result column="config_type" property="config_type" />  
         <result column="config_content" property="config_content" />  
         <result column="config_value" property="config_value" />  
         <result column="config_default_value" property="config_default_value" />  
         <result column="config_remark" property="config_remark" />  
         <result column="config_parse" property="config_parse" />  
    </resultMap>
    
    <sql id="sql">
         id,
         config_id,
         config_name,
         config_type,
         config_content,
         config_value,
         config_default_value,
         config_remark,
         config_parse
    </sql>
    
    <sql id="insertSql">
         config_id,
         config_name,
         config_type,
         config_content,
         config_value,
         config_default_value,
         config_remark,
         config_parse
    </sql>
    
    <sql id="unionSql">
         b.id,
         b.config_id,
         b.config_name,
         b.config_type,
         b.config_content,
         b.config_value,
         b.config_default_value,
         b.config_remark,
         b.config_parse
    </sql>
    
    <!-- 当前表条件 -->
    <sql id = "whereSql">
	   <where>
            <if test="id != null and id != '' ">
	          and id = #{id}
	        </if>
            <if test="config_id != null and config_id != '' ">
	          and config_id = #{config_id}
	        </if>
            <if test="config_name != null and config_name != '' ">
	          and config_name = #{config_name}
	        </if>
            <if test="config_type != null and config_type != '' ">
	          and config_type = #{config_type}
	        </if>
            <if test="config_content != null and config_content != '' ">
	          and config_content = #{config_content}
	        </if>
            <if test="config_value != null and config_value != '' ">
	          and config_value = #{config_value}
	        </if>
            <if test="config_default_value != null and config_default_value != '' ">
	          and config_default_value = #{config_default_value}
	        </if>
            <if test="config_remark != null and config_remark != '' ">
	          and config_remark = #{config_remark}
	        </if>
            <if test="config_parse != null and config_parse != '' ">
	          and config_parse = #{config_parse}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表(或不存在当前表)条件(Map参数) -->
    <sql id = "whereMapSql">
	   <where>
            <if test="id != null and id != '' ">
	          and b.id = #{id}
	        </if>
            <if test="config_id != null and config_id != '' ">
	          and b.config_id = #{config_id}
	        </if>
            <if test="config_name != null and config_name != '' ">
	          and b.config_name = #{config_name}
	        </if>
            <if test="config_type != null and config_type != '' ">
	          and b.config_type = #{config_type}
	        </if>
            <if test="config_content != null and config_content != '' ">
	          and b.config_content = #{config_content}
	        </if>
            <if test="config_value != null and config_value != '' ">
	          and b.config_value = #{config_value}
	        </if>
            <if test="config_default_value != null and config_default_value != '' ">
	          and b.config_default_value = #{config_default_value}
	        </if>
            <if test="config_remark != null and config_remark != '' ">
	          and b.config_remark = #{config_remark}
	        </if>
            <if test="config_parse != null and config_parse != '' ">
	          and b.config_parse = #{config_parse}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表联合查询条件(Map参数) -->
    <sql id = "whereUnionSql">
	   <where>
            <if test="id != null and id != '' ">
	          and b.id = #{id}
	        </if>
            <if test="config_id != null and config_id != '' ">
	          and b.config_id = #{config_id}
	        </if>
            <if test="config_name != null and config_name != '' ">
	          and b.config_name = #{config_name}
	        </if>
            <if test="config_type != null and config_type != '' ">
	          and b.config_type = #{config_type}
	        </if>
            <if test="config_content != null and config_content != '' ">
	          and b.config_content = #{config_content}
	        </if>
            <if test="config_value != null and config_value != '' ">
	          and b.config_value = #{config_value}
	        </if>
            <if test="config_default_value != null and config_default_value != '' ">
	          and b.config_default_value = #{config_default_value}
	        </if>
            <if test="config_remark != null and config_remark != '' ">
	          and b.config_remark = #{config_remark}
	        </if>
            <if test="config_parse != null and config_parse != '' ">
	          and b.config_parse = #{config_parse}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表批量删除条件 -->
    <sql id="whereBatchSql">
		<where>
			<if test="id != null and id != '' ">
				and id in
				<foreach collection="id" item="id" separator="," open="("
					close=")">
					#{id}
				</foreach>
			</if>
		</where>
	</sql>
	
	<!-- 分页sql条件 -->
	<sql id="pageSql">
       <if test="begin == 0 || begin > 0">
             limit #{begin},#{end}
        </if>
    </sql>

	<!-- 当前表添加-->
	<insert id="insert" parameterType="com.yunwei.product.common.model.YwBaseconfig">
		insert into yw_baseconfig(<include refid="insertSql"/>) 
		values(
	             #{config_id},
	             #{config_name},
	             #{config_type},
	             #{config_content},
	             #{config_value},
	             #{config_default_value},
	             #{config_remark},
	             #{config_parse}
		     )
	</insert>
	
	<!-- 当前表批量添加 -->
	<insert id="insertBatch" parameterType="java.util.List">
        insert into yw_baseconfig(<include refid="insertSql"/>)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
	             #{item.config_id},
	             #{item.config_name},
	             #{item.config_type},
	             #{item.config_content},
	             #{item.config_value},
	             #{item.config_default_value},
	             #{item.config_remark},
	             #{item.config_parse}
	       )
        </foreach>
    </insert>
    
	<!-- 当前表查询 -->
	<select id="queryList" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.YwBaseconfig">
		select <include refid="sql"/> from yw_baseconfig
		<include refid="whereSql"/>
		order by id
	</select>
	
	<!-- 当前表分页查询(Map参数) -->
	<select id="queryListPage" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.YwBaseconfig">
		select <include refid="sql"/> from yw_baseconfig
		<include refid="whereSql"/>
		order by id desc
		limit #{begin},#{end}
	</select>
	
	<!-- 当前表联合查询(或分页联合查询)(Map参数)(1、子表关联查询，2、不同类型表关联查询) -->
	<select id="queryUnionList" parameterType="java.util.Map" resultMap="resultMap">
		select <include refid="unionSql"/> from yw_baseconfig as b
		<include refid="whereUnionSql"/>
		<!-- 分页查询，通过begin条件区别 -->
		<include refid="pageSql"/>
		order by id
	</select>
	
	<!-- 当前表查询总条数 -->
	<select id="queryTotals" parameterType="java.util.Map" resultType="int">
		select count(1) from yw_baseconfig
		<include refid="whereSql"/>
	</select>

	<!-- 当前表更新 -->
	<update id="update" parameterType="java.util.Map">
	    update yw_baseconfig
	     <trim prefix="set" suffixOverrides=",">
            <if test="config_name != null and config_name != '' ">
	           config_name = #{config_name},
	        </if>
            <if test="config_type != null and config_type != '' ">
	           config_type = #{config_type},
	        </if>
            <if test="config_content != null and config_content != '' ">
	           config_content = #{config_content},
	        </if>
            <if test="config_value != null and config_value != '' ">
	           config_value = #{config_value},
	        </if>
            <if test="config_default_value != null and config_default_value != '' ">
	           config_default_value = #{config_default_value},
	        </if>
            <if test="config_remark != null and config_remark != '' ">
	           config_remark = #{config_remark},
	        </if>
            <if test="config_parse != null and config_parse != '' ">
	           config_parse = #{config_parse},
	        </if>
	    </trim>
	    <where>
             <if test="id != null and id != '' ">
	           and id = #{id}
	         </if>
	         <if test="config_id != null and config_id != '' ">
	           and config_id = #{config_id}
	         </if>
	    </where>
	</update>
	
	<!-- 当前表删除 -->
	<delete id="delete" parameterType="java.util.Map">
	    delete from yw_baseconfig
	    <where>
            <if test="id != null and id != '' ">
	           and id = #{id}
	        </if>
	    </where>
	</delete>
	
	<!-- 当前批量删除 -->
	<delete id="deleteBatch" parameterType="java.util.Map">
	   delete from yw_baseconfig
	   <include refid="whereBatchSql"/>
	</delete>
	
</mapper>