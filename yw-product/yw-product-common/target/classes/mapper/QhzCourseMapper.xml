<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunwei.product.common.dao.QhzCourseDao">

    <!-- 过滤查询结果(主要用于联合查询) -->
    <resultMap type="com.yunwei.product.common.model.QhzCourse" id="resultMap">
         <id column="id" property="id" />
         <result column="title" property="title" />  
         <result column="img" property="img" />  
         <result column="video" property="video" />  
         <result column="video_time_length" property="video_time_length" />  
         <result column="epis" property="epis" />  
         <result column="curriculum_id" property="curriculum_id" />  
         <result column="teacher_id" property="teacher_id" />  
         <result column="resume" property="resume" />  
         <result column="content" property="content" />  
         <result column="create_name" property="create_name" />  
         <result column="create_time" property="create_time" />  
         <result column="update_name" property="update_name" />  
         <result column="update_time" property="update_time" />  
         <result column="unit_price" property="unit_price" />  
         <result column="fic_price" property="fic_price" />  
         <result column="click_num" property="click_num" />  
         <result column="fic_click_num" property="fic_click_num" />  
         <result column="audition" property="audition" />  
         <result column="status" property="status" />  
    </resultMap>
    
    <sql id="sql">
         id,
         title,
         img,
         video,
         video_time_length,
         epis,
         curriculum_id,
         teacher_id,
         resume,
         content,
         create_name,
         create_time,
         update_name,
         update_time,
         unit_price,
         fic_price,
         click_num,
         fic_click_num,
         audition,
         status
    </sql>
    
    <sql id="insertSql">
         title,
         img,
         video,
         video_time_length,
         epis,
         curriculum_id,
         teacher_id,
         resume,
         content,
         create_name,
         create_time,
         update_name,
         update_time,
         unit_price,
         fic_price,
         click_num,
         fic_click_num,
         audition,
         status
    </sql>
    
    <sql id="unionSql">
         c.id,
         c.title,
         c.img,
         c.video,
         c.video_time_length,
         c.epis,
         c.curriculum_id,
         c.teacher_id,
         c.resume,
         c.content,
         c.create_name,
         c.create_time,
         c.update_name,
         c.update_time,
         c.unit_price,
         c.fic_price,
         c.click_num,
         c.fic_click_num,
         c.audition,
         c.status
    </sql>
    
    <!-- 当前表参数条件 -->
    <sql id = "whereSql">
	   <where>
            <if test="id != null and id != '' ">
	          and id = #{id}
	        </if>
            <if test="title != null and title != '' ">
	          and title = #{title}
	        </if>
            <if test="img != null and img != '' ">
	          and img = #{img}
	        </if>
            <if test="video != null and video != '' ">
	          and video = #{video}
	        </if>
            <if test="video_time_length != null and video_time_length != '' ">
	          and video_time_length = #{video_time_length}
	        </if>
            <if test="epis != null and epis != '' ">
	          and epis = #{epis}
	        </if>
            <if test="curriculum_id != null and curriculum_id != '' ">
	          and curriculum_id = #{curriculum_id}
	        </if>
            <if test="teacher_id != null and teacher_id != '' ">
	          and teacher_id = #{teacher_id}
	        </if>
            <if test="resume != null and resume != '' ">
	          and resume = #{resume}
	        </if>
            <if test="content != null and content != '' ">
	          and content = #{content}
	        </if>
            <if test="create_name != null and create_name != '' ">
	          and create_name = #{create_name}
	        </if>
            <if test="create_time != null and create_time != '' ">
	          and create_time = #{create_time}
	        </if>
            <if test="update_name != null and update_name != '' ">
	          and update_name = #{update_name}
	        </if>
            <if test="update_time != null and update_time != '' ">
	          and update_time = #{update_time}
	        </if>
            <if test="unit_price != null and unit_price != '' ">
	          and unit_price = #{unit_price}
	        </if>
            <if test="fic_price != null and fic_price != '' ">
	          and fic_price = #{fic_price}
	        </if>
            <if test="click_num != null and click_num != '' ">
	          and click_num = #{click_num}
	        </if>
            <if test="fic_click_num != null and fic_click_num != '' ">
	          and fic_click_num = #{fic_click_num}
	        </if>
            <if test="audition != null and audition != '' ">
	          and audition = #{audition}
	        </if>
            <if test="status != null and status != '' ">
	          and status = #{status}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表联合查询条件(Map参数) -->
    <sql id = "whereUnionSql">
	   <where>
            <if test="id != null and id != '' ">
	          and c.id = #{id}
	        </if>
            <if test="title != null and title != '' ">
	          and c.title = #{title}
	        </if>
            <if test="img != null and img != '' ">
	          and c.img = #{img}
	        </if>
            <if test="video != null and video != '' ">
	          and c.video = #{video}
	        </if>
            <if test="video_time_length != null and video_time_length != '' ">
	          and c.video_time_length = #{video_time_length}
	        </if>
            <if test="epis != null and epis != '' ">
	          and c.epis = #{epis}
	        </if>
            <if test="curriculum_id != null and curriculum_id != '' ">
	          and c.curriculum_id = #{curriculum_id}
	        </if>
            <if test="teacher_id != null and teacher_id != '' ">
	          and c.teacher_id = #{teacher_id}
	        </if>
            <if test="resume != null and resume != '' ">
	          and c.resume = #{resume}
	        </if>
            <if test="content != null and content != '' ">
	          and c.content = #{content}
	        </if>
            <if test="create_name != null and create_name != '' ">
	          and c.create_name = #{create_name}
	        </if>
            <if test="create_time != null and create_time != '' ">
	          and c.create_time = #{create_time}
	        </if>
            <if test="update_name != null and update_name != '' ">
	          and c.update_name = #{update_name}
	        </if>
            <if test="update_time != null and update_time != '' ">
	          and c.update_time = #{update_time}
	        </if>
            <if test="unit_price != null and unit_price != '' ">
	          and c.unit_price = #{unit_price}
	        </if>
            <if test="fic_price != null and fic_price != '' ">
	          and c.fic_price = #{fic_price}
	        </if>
            <if test="click_num != null and click_num != '' ">
	          and c.click_num = #{click_num}
	        </if>
            <if test="fic_click_num != null and fic_click_num != '' ">
	          and c.fic_click_num = #{fic_click_num}
	        </if>
            <if test="audition != null and audition != '' ">
	          and c.audition = #{audition}
	        </if>
            <if test="status != null and status != '' ">
	          and c.status = #{status}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表批量删除条件 -->
    <sql id="whereBatchSql">
		<where>
			<if test="id != null and id != '' ">
				and id in
				<foreach collection="id" item="id" separator="," open="("
					close=")">
					#{id}
				</foreach>
			</if>
		</where>
	</sql>
	
	<!-- 分页sql条件 -->
	<sql id="pageSql">
       <if test="begin == 0 || begin > 0">
             limit #{begin},#{end}
        </if>
    </sql>

	<!-- 当前表添加-->
	<insert id="insert" parameterType="com.yunwei.product.common.model.QhzCourse">
		insert into qhz_course(<include refid="insertSql"/>) 
		values(
	             #{title},
	             #{img},
	             #{video},
	             #{video_time_length},
	             #{epis},
	             #{curriculum_id},
	             #{teacher_id},
	             #{resume},
	             #{content},
	             #{create_name},
	             #{create_time},
	             #{update_name},
	             #{update_time},
	             #{unit_price},
	             #{fic_price},
	             #{click_num},
	             #{fic_click_num},
	             #{audition},
	             #{status}
		     )
	</insert>
	
	<!-- 当前表批量添加 -->
	<insert id="insertBatch" parameterType="java.util.List">
        insert into qhz_course(<include refid="insertSql"/>)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
	             #{item.title},
	             #{item.img},
	             #{item.video},
	             #{item.video_time_length},
	             #{item.epis},
	             #{item.curriculum_id},
	             #{item.teacher_id},
	             #{item.resume},
	             #{item.content},
	             #{item.create_name},
	             #{item.create_time},
	             #{item.update_name},
	             #{item.update_time},
	             #{item.unit_price},
	             #{item.fic_price},
	             #{item.click_num},
	             #{item.fic_click_num},
	             #{item.audition},
	             #{item.status}
	       )
        </foreach>
    </insert>
    
	<!-- 当前表查询 -->
	<select id="queryList" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.QhzCourse">
		select <include refid="sql"/> from qhz_course
		<include refid="whereSql"/>
		order by id desc
	</select>
	
	<!-- 当前表分页查询(Map参数) -->
	<select id="queryListPage" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.QhzCourse">
		select <include refid="sql"/> from qhz_course
		<include refid="whereSql"/>
		order by id desc
		limit #{begin},#{end}
	</select>
	
	<!-- 当前表联合查询(Map参数)(1、子表关联查询，2、不同类型表关联查询) -->
	<select id="queryUnionList" parameterType="java.util.Map" resultMap="resultMap">
		select <include refid="unionSql"/> from qhz_course as c
		<include refid="whereUnionSql"/>
		order by id desc
	</select>
	
	<!-- 当前表联合分页查询)(Map参数)(1、子表关联查询，2、不同类型表关联查询) -->
	<select id="queryUnionListPage" parameterType="java.util.Map" resultMap="resultMap">
		select <include refid="unionSql"/>,qc.title as curriculum_name,qt.name as teacher_name from qhz_course as c
			left join qhz_curriculum qc on c.curriculum_id = qc.id
			left join qhz_teacher qt on c.teacher_id = qt.id
		<include refid="whereUnionSql"/>
		<!-- 分页查询，通过begin条件区别 -->
		order by c.id desc
		<include refid="pageSql"/>
	</select>
	
	<!-- 当前表查询总条数 -->
	<select id="queryTotals" parameterType="java.util.Map" resultType="int">
		select count(1) from qhz_course
		<include refid="whereSql"/>
	</select>
	
	<!-- 当前表更新 -->
	<update id="update" parameterType="java.util.Map">
	    update qhz_course
	     <trim prefix="set" suffixOverrides=",">
            <if test="title != null and title != '' ">
	           title = #{title},
	        </if>
            <if test="img != null and img != '' ">
	           img = #{img},
	        </if>
            <if test="video != null ">
	           video = #{video},
	        </if>
            <if test="video_time_length != null and video_time_length != '' ">
	           video_time_length = #{video_time_length},
	        </if>
            <if test="epis != null and epis != '' ">
	           epis = #{epis},
	        </if>
            <if test="curriculum_id != null and curriculum_id != '' ">
	           curriculum_id = #{curriculum_id},
	        </if>
            <if test="teacher_id != null and teacher_id != '' ">
	           teacher_id = #{teacher_id},
	        </if>
            <if test="resume != null and resume != '' ">
	           resume = #{resume},
	        </if>
            <if test="content != null and content != '' ">
	           content = #{content},
	        </if>
            <if test="create_name != null and create_name != '' ">
	           create_name = #{create_name},
	        </if>
            <if test="create_time != null and create_time != '' ">
	           create_time = #{create_time},
	        </if>
            <if test="update_name != null and update_name != '' ">
	           update_name = #{update_name},
	        </if>
            <if test="update_time != null and update_time != '' ">
	           update_time = #{update_time},
	        </if>
            <if test="unit_price != null and unit_price != '' ">
	           unit_price = #{unit_price},
	        </if>
            <if test="fic_price != null and fic_price != '' ">
	           fic_price = #{fic_price},
	        </if>
            <if test="click_num != null and click_num != '' ">
	           click_num = #{click_num},
	        </if>
            <if test="fic_click_num != null and fic_click_num != '' ">
	           fic_click_num = #{fic_click_num},
	        </if>
            <if test="audition != null and audition != '' ">
	           audition = #{audition},
	        </if>
            <if test="status != null and status != '' ">
	           status = #{status},
	        </if>
	    </trim>
	    <where>
             <if test="id != null and id != '' ">
	           and id = #{id}
	         </if>
	    </where>
	</update>
	
	<!-- 当前表批量更新 -->
	<update id="updateBatch">
	     update qhz_course
	     <trim prefix="set" suffixOverrides=",">
	        <foreach collection="fields" item="field" separator="end,">
	          ${field} = CASE id
	          <foreach collection="lists" item="item">
	              when #{item.id} then #{item.${field}}
	          </foreach>
	        </foreach>
	     </trim>
	     <where>
	         and id in
	        <foreach collection="lists" item="item" separator="," open="(" close=")">
	             #{item.id}
	        </foreach>
	     </where>
	</update>
	
	<!-- 当前表删除 -->
	<delete id="delete" parameterType="java.util.Map">
	    delete from qhz_course
	    <where>
            <if test="id != null and id != '' ">
	           and id = #{id}
	        </if>
	    </where>
	</delete>
	
	<!-- 当前批量删除 -->
	<delete id="deleteBatch" parameterType="java.util.Map">
	   delete from qhz_course
	   <include refid="whereBatchSql"/>
	</delete>
	
</mapper>