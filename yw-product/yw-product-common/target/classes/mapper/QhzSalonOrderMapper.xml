<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunwei.product.common.dao.QhzSalonOrderDao">

    <!-- 过滤查询结果(主要用于联合查询) -->
    <resultMap type="com.yunwei.product.common.model.QhzSalonOrder" id="resultMap">
         <id column="id" property="id" />
         <result column="openid" property="openid" />  
         <result column="order_sn" property="order_sn" />  
         <result column="order_type" property="order_type" />  
         <result column="order_total" property="order_total" />  
         <result column="order_realtotal" property="order_realtotal" />  
         <result column="order_curriculums" property="order_curriculums" />  
         <result column="order_adviser" property="order_adviser" />  
         <result column="order_sc_price" property="order_sc_price" />  
         <result column="create_datetime" property="create_datetime" />  
         <result column="update_datetime" property="update_datetime" />  
         <result column="order_name" property="order_name" />  
         <result column="order_tel" property="order_tel" />  
         <result column="order_benefit_name" property="order_benefit_name" />  
         <result column="order_benefit_card" property="order_benefit_card" />  
         <result column="order_address" property="order_address" />  
         <result column="order_remark" property="order_remark" />  
         <result column="status" property="status" />  
    </resultMap>
    
    <sql id="sql">
         id,
         openid,
         order_sn,
         order_type,
         order_total,
         order_realtotal,
         order_curriculums,
         order_adviser,
         order_sc_price,
         create_datetime,
         update_datetime,
         order_name,
         order_tel,
         order_benefit_name,
         order_benefit_card,
         order_address,
         order_remark,
         status
    </sql>
    
    <sql id="insertSql">
         openid,
         order_sn,
         order_type,
         order_total,
         order_realtotal,
         order_curriculums,
         order_adviser,
         order_sc_price,
         create_datetime,
         update_datetime,
         order_name,
         order_tel,
         order_benefit_name,
         order_benefit_card,
         order_address,
         order_remark,
         status
    </sql>
    
    <sql id="unionSql">
         so.id,
         so.openid,
         so.order_sn,
         so.order_type,
         so.order_total,
         so.order_realtotal,
         so.order_curriculums,
         so.order_adviser,
         so.order_sc_price,
         so.create_datetime,
         so.update_datetime,
         so.order_name,
         so.order_tel,
         so.order_benefit_name,
         so.order_benefit_card,
         so.order_address,
         so.order_remark,
         so.status
    </sql>
    
    <!-- 当前表参数条件 -->
    <sql id = "whereSql">
	   <where>
            <if test="id != null and id != '' ">
	          and id = #{id}
	        </if>
            <if test="openid != null and openid != '' ">
	          and openid = #{openid}
	        </if>
            <if test="order_sn != null and order_sn != '' ">
	          and order_sn = #{order_sn}
	        </if>
            <if test="order_type != null and order_type != '' ">
	          and order_type = #{order_type}
	        </if>
            <if test="order_total != null and order_total != '' ">
	          and order_total = #{order_total}
	        </if>
            <if test="order_realtotal != null and order_realtotal != '' ">
	          and order_realtotal = #{order_realtotal}
	        </if>
            <if test="order_curriculums != null and order_curriculums != '' ">
	          and order_curriculums = #{order_curriculums}
	        </if>
	        <if test="order_adviser != null and order_adviser != '' ">
	          and order_adviser = #{order_adviser}
	        </if>
	        <if test="order_sc_price != null and order_sc_price != '' ">
	          and order_sc_price = #{order_sc_price}
	        </if>
            <if test="create_datetime != null and create_datetime != '' ">
	          and create_datetime = #{create_datetime}
	        </if>
            <if test="update_datetime != null and update_datetime != '' ">
	          and update_datetime = #{update_datetime}
	        </if>
            <if test="order_name != null and order_name != '' ">
	          and order_name = #{order_name}
	        </if>
            <if test="order_tel != null and order_tel != '' ">
	          and order_tel = #{order_tel}
	        </if>
	        <if test="order_benefit_name != null and order_benefit_name != '' ">
	          and order_benefit_name = #{order_benefit_name}
	        </if>
	        <if test="order_benefit_card != null and order_benefit_card != '' ">
	          and order_benefit_card = #{order_benefit_card}
	        </if>
            <if test="order_address != null and order_address != '' ">
	          and order_address = #{order_address}
	        </if>
            <if test="order_remark != null and order_remark != '' ">
	          and order_remark = #{order_remark}
	        </if>
            <if test="status != null and status != '' ">
	          and status = #{status}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表联合查询条件(Map参数) -->
    <sql id = "whereUnionSql">
	   <where>
            <if test="id != null and id != '' ">
	          and so.id = #{id}
	        </if>
            <if test="openid != null and openid != '' ">
	          and so.openid = #{openid}
	        </if>
            <if test="order_sn != null and order_sn != '' ">
	          and so.order_sn = #{order_sn}
	        </if>
            <if test="order_type != null and order_type != '' ">
	          and so.order_type = #{order_type}
	        </if>
            <if test="order_total != null and order_total != '' ">
	          and so.order_total = #{order_total}
	        </if>
            <if test="order_realtotal != null and order_realtotal != '' ">
	          and so.order_realtotal = #{order_realtotal}
	        </if>
            <if test="order_curriculums != null and order_curriculums != '' ">
	          and so.order_curriculums = #{order_curriculums}
	        </if>
	        <if test="order_adviser != null and order_adviser != '' ">
	          and so.order_adviser = #{order_adviser}
	        </if>
	        <if test="order_sc_price != null and order_sc_price != '' ">
	          and so.order_sc_price = #{order_sc_price}
	        </if>
            <if test="create_datetime != null and create_datetime != '' ">
	          and so.create_datetime = #{create_datetime}
	        </if>
            <if test="update_datetime != null and update_datetime != '' ">
	          and so.update_datetime = #{update_datetime}
	        </if>
            <if test="order_name != null and order_name != '' ">
	          and so.order_name = #{order_name}
	        </if>
            <if test="order_tel != null and order_tel != '' ">
	          and so.order_tel = #{order_tel}
	        </if>
	        <if test="order_benefit_name != null and order_benefit_name != '' ">
	          and so.order_benefit_name = #{order_benefit_name}
	        </if>
	        <if test="order_benefit_card != null and order_benefit_card != '' ">
	          and so.order_benefit_card = #{order_benefit_card}
	        </if>
            <if test="order_address != null and order_address != '' ">
	          and so.order_address = #{order_address}
	        </if>
            <if test="order_remark != null and order_remark != '' ">
	          and so.order_remark = #{order_remark}
	        </if>
            <if test="status != null and status != '' ">
	          and so.status = #{status}
	        </if>
	    </where> 
    </sql>
    
    <!-- 当前表批量删除条件 -->
    <sql id="whereBatchSql">
		<where>
			<if test="id != null and id != '' ">
				and id in
				<foreach collection="id" item="id" separator="," open="("
					close=")">
					#{id}
				</foreach>
			</if>
		</where>
	</sql>
	
	<!-- 分页sql条件 -->
	<sql id="pageSql">
       <if test="begin == 0 || begin > 0">
             limit #{begin},#{end}
        </if>
    </sql>

	<!-- 当前表添加-->
	<insert id="insert" parameterType="com.yunwei.product.common.model.QhzSalonOrder">
		insert into qhz_salon_order(<include refid="insertSql"/>) 
		values(
	             #{openid},
	             #{order_sn},
	             #{order_type},
	             #{order_total},
	             #{order_realtotal},
	             #{order_curriculums},
	             #{order_adviser},
	             #{order_sc_price},
	             #{create_datetime},
	             #{update_datetime},
	             #{order_name},
	             #{order_tel},
	             #{order_benefit_name},
	             #{order_benefit_card},
	             #{order_address},
	             #{order_remark},
	             #{status}
		     )
	</insert>
	
	<!-- 当前表批量添加 -->
	<insert id="insertBatch" parameterType="java.util.List">
        insert into qhz_salon_order(<include refid="insertSql"/>)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (
	             #{item.openid},
	             #{item.order_sn},
	             #{item.order_type},
	             #{item.order_total},
	             #{item.order_realtotal},
	             #{item.order_curriculums},
	             #{item.order_adviser},
	             #{item.order_sc_price},
	             #{item.create_datetime},
	             #{item.update_datetime},
	             #{item.order_name},
	             #{item.order_tel},
	             #{item.order_benefit_name},
	             #{item.order_benefit_card},
	             #{item.order_address},
	             #{item.order_remark},
	             #{item.status}
	       )
        </foreach>
    </insert>
    
	<!-- 当前表查询 -->
	<select id="queryList" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.QhzSalonOrder">
		select <include refid="sql"/> from qhz_salon_order
		<include refid="whereSql"/>
		order by id desc
	</select>
	
	<!-- 当前表分页查询(Map参数) -->
	<select id="queryListPage" parameterType="java.util.Map" resultType="com.yunwei.product.common.model.QhzSalonOrder">
		select <include refid="sql"/> from qhz_salon_order
		<include refid="whereSql"/>
		order by id desc
		limit #{begin},#{end}
	</select>
	
	<!-- 当前表联合查询(Map参数)(1、子表关联查询，2、不同类型表关联查询) -->
	<select id="queryUnionList" parameterType="java.util.Map" resultMap="resultMap">
		select <include refid="unionSql"/> from qhz_salon_order as so
		<include refid="whereUnionSql"/>
		order by id desc
	</select>
	
	<!-- 当前表联合分页查询)(Map参数)(1、子表关联查询，2、不同类型表关联查询) -->
	<select id="queryUnionListPage" parameterType="java.util.Map" resultMap="resultMap">
		select <include refid="unionSql"/> from qhz_salon_order as so
		<include refid="whereUnionSql"/>
		<!-- 分页查询，通过begin条件区别 -->
		<include refid="pageSql"/>
		order by id desc
	</select>
	
	<!-- 当前表查询总条数 -->
	<select id="queryTotals" parameterType="java.util.Map" resultType="int">
		select count(1) from qhz_salon_order
		<include refid="whereSql"/>
	</select>
	
	<!-- 当前表更新 -->
	<update id="update" parameterType="java.util.Map">
	    update qhz_salon_order
	     <trim prefix="set" suffixOverrides=",">
            <if test="openid != null and openid != '' ">
	           openid = #{openid},
	        </if>
            <if test="order_sn != null and order_sn != '' ">
	           order_sn = #{order_sn},
	        </if>
            <if test="order_type != null and order_type != '' ">
	           order_type = #{order_type},
	        </if>
            <if test="order_total != null and order_total != '' ">
	           order_total = #{order_total},
	        </if>
            <if test="order_realtotal != null and order_realtotal != '' ">
	           order_realtotal = #{order_realtotal},
	        </if>
            <if test="order_curriculums != null and order_curriculums != '' ">
	           order_curriculums = #{order_curriculums},
	        </if>
	        <if test="order_adviser != null and order_adviser != '' ">
	           order_adviser = #{order_adviser},
	        </if>
	        <if test="order_sc_price != null and order_sc_price != '' ">
	           order_sc_price = #{order_sc_price},
	        </if>
            <if test="create_datetime != null and create_datetime != '' ">
	           create_datetime = #{create_datetime},
	        </if>
            <if test="update_datetime != null and update_datetime != '' ">
	           update_datetime = #{update_datetime},
	        </if>
            <if test="order_name != null and order_name != '' ">
	           order_name = #{order_name},
	        </if>
            <if test="order_tel != null and order_tel != '' ">
	           order_tel = #{order_tel},
	        </if>
	        <if test="order_benefit_name != null and order_benefit_name != '' ">
	           order_benefit_name = #{order_benefit_name},
	        </if>
	        <if test="order_benefit_card != null and order_benefit_card != '' ">
	           order_benefit_card = #{order_benefit_card},
	        </if>
            <if test="order_address != null and order_address != '' ">
	           order_address = #{order_address},
	        </if>
            <if test="order_remark != null and order_remark != '' ">
	           order_remark = #{order_remark},
	        </if>
            <if test="status != null and status != '' ">
	           status = #{status},
	        </if>
	    </trim>
	    <where>
             <if test="id != null and id != '' ">
	           and id = #{id}
	         </if>
	    </where>
	</update>
	
	<!-- 当前表批量更新 -->
	<update id="updateBatch">
	     update qhz_salon_order
	     <trim prefix="set" suffixOverrides=",">
	        <foreach collection="fields" item="field" separator="end,">
	          ${field} = CASE id
	          <foreach collection="lists" item="item">
	              when #{item.id} then #{item.${field}}
	          </foreach>
	        </foreach>
	     </trim>
	     <where>
	         and id in
	        <foreach collection="lists" item="item" separator="," open="(" close=")">
	             #{item.id}
	        </foreach>
	     </where>
	</update>
	
	<!-- 当前表删除 -->
	<delete id="delete" parameterType="java.util.Map">
	    delete from qhz_salon_order
	    <where>
            <if test="id != null and id != '' ">
	           and id = #{id}
	        </if>
	    </where>
	</delete>
	
	<!-- 当前批量删除 -->
	<delete id="deleteBatch" parameterType="java.util.Map">
	   delete from qhz_salon_order
	   <include refid="whereBatchSql"/>
	</delete>
	
</mapper>